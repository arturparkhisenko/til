// node inspect debugging.js (debug command was deprecated)
// repl - command

let person = {
  name: 'artur'
};

person.age = 28;

// debugger; // works well

person.name = 'Artur';

console.log(person);
